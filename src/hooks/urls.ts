export enum Catbox {
    UploadURL = "https://catbox.moe/user/api.php"
}

export enum SMMS {
    UploadURL = "https://smms.app/api/v2/upload"
}

export enum ImgURL {
    UploadPath = "/api/v2/upload"
}

export enum Chevereto {
    UploadPath = "/api/1/upload"
}

export enum Imgur {
    UploadURL = "https://api.imgur.com/3/upload",
    AuthorizeURL = "https://api.imgur.com/oauth2/authorize",
    TokenURL = "https://api.imgur.com/oauth2/token"
}